// const { EventEmitter } = require("events");

// const emitter = new EventEmitter();

// const messageCallback = (message) => {
// 	console.log(message);
// };

// emitter.on("message", messageCallback); // or u can use once for once call event

// const MESSAGE = process.env.MESSAGE || "";

// emitter.emit("message", MESSAGE || `can't find message`);

// // emitter.removeListener("message", messageCallback); // remove certain message

// // emitter.removeAllListeners(); // remove all events listener

// console.log(emitter.eventNames()); // get all events listener : ['message']
